# Local/dev example only.
#
# This file is intentionally named `terraform.tfvars.example` so it is not
# picked up automatically by Terraform or checked into state. For real usage,
# prefer `TF_VAR_*` environment variables (see README.md) or create your own
# local `terraform.tfvars` (gitignored).

database_host               = "postgres-permesi"
# For TLS, use "require", "verify-ca", or "verify-full" (recommended for production).
# When using verify-ca/verify-full, ensure the Postgres CA cert is available to Vault.
database_sslmode            = "disable"
permesi_database_username   = "vault_permesi"
permesi_database_password   = "change-me"
genesis_database_username   = "vault_genesis"
genesis_database_password   = "change-me"

meta {
  name: Create Organization
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/v1/orgs
  body: json
  auth: none
}

headers {
  Cookie: permesi_session={{sessionCookie}}
}

body:json {
  {
    "name": "Acme Corp",
    "slug": "acme"
  }
}

# Required
# Base DSN (username/password are injected by Vault via the Agent Proxy)
GENESIS_DSN=postgres://127.0.0.1:5432/genesis

# Vault Agent Connection (Sidecar Mode)
# No RoleID/SecretID required; the Agent handles authentication.
# GENESIS_VAULT_URL defaults to unix:///run/genesis/agent.sock in systemd service.
# GENESIS_VAULT_URL=unix:///run/genesis/agent.sock

# TLS PEM bundle (Key + Cert + CA)
# GENESIS_TLS_PEM_BUNDLE=/run/genesis/tls.bundle.pem

# Optional
GENESIS_PORT=8000
GENESIS_LOG_LEVEL=info
GENESIS_ADMISSION_ISS=https://genesis.permesi.localhost
GENESIS_ADMISSION_AUD=permesi
GENESIS_TRANSIT_MOUNT=transit/genesis
GENESIS_IP_HEADER=CF-Connecting-IP
GENESIS_COUNTRY_HEADER=CF-IPCountry
